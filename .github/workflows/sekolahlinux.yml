name: Workflow Sekolahlinux

on:
  push:
    branches:
      - main

jobs:
  dev_packaging_stage:
    runs-on: sekolahlinux-runner
    container: ubuntu:20.04
    steps:
      - name: task2
        uses: actions/checkout@v3
      - name: task1
        run: |
          echo "hello world1"
          echo "hello world2"
          echo "hello world3"

